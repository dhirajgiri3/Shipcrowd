import { ReturnsClient } from './components/ReturnsClient';

export const metadata = {
    title: 'Returns Management | ShipCrowd Admin',
    description: 'Monitor and manage customer returns across all companies'
};

export default function ReturnsPage() { return <ReturnsClient />; }
