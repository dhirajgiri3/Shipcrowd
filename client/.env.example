# ===============================================
# ENVIRONMENT VARIABLES EXAMPLE
# ===============================================
# Copy this file to .env.development for local development
# Copy this file to .env.production for production deployment

# API Base URL (backend server)
# Development: http://localhost:5005/api/v1
# Production: https://api.Shipcrowd.com/v1
NEXT_PUBLIC_API_URL=http://localhost:5005/api/v1

# App Version (for cache busting)
# Increment this when you make breaking API changes to invalidate client-side cache
# Format: MAJOR.MINOR.PATCH (e.g., 1.0.0, 1.1.0, 2.0.0)
NEXT_PUBLIC_APP_VERSION=1.0.0

# Seller order-centric shipping UX flag
# Default behavior: enabled when missing/undefined.
# Set to 'false' to fallback to legacy shipment-centric create flow.
NEXT_PUBLIC_SELLER_ORDER_CENTRIC_SHIPPING=true

# Development Auth Bypass (DEVELOPMENT ONLY!)
# Set to 'true' ONLY in .env.development to bypass auth for /seller and /admin
# NEVER set this in production - it's a critical security risk
# NEXT_PUBLIC_DEV_BYPASS_AUTH=true



# ===============================================
# SETUP INSTRUCTIONS
# ===============================================
# 1. Copy this file: cp .env.example .env.development
# 2. Set NEXT_PUBLIC_API_URL=http://localhost:5005/api/v1
# 3. Set NEXT_PUBLIC_DEV_BYPASS_AUTH=true (optional, for easier development)
# 4. For production, create .env.production with production API URL
# 5. NEVER set NEXT_PUBLIC_DEV_BYPASS_AUTH in production!
